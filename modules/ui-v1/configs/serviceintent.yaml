service: "ui"
module: "ui-v1"

target:
  base_url: "http://app-ui-v1:8080"
  health_endpoint: "/health"
  metrics_endpoint: "/metrics"

sla:
  max_latency_ms: 500
  max_error_rate: 0.01

deployment:
  candidate_image: "microservices-qa-ci-cd-lab-app-ui-v1:latest"

